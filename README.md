# MNIST_ML_AI_project
A from-scratch neural network implementation using NumPy to classify handwritten digits from the MNIST dataset. Includes forward propagation, backpropagation, gradient descent, and visualization of predictions without using high-level ML frameworks.
